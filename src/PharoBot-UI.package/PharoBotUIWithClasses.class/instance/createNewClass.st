actions
createNewClass

	"Pedir nombre con un popup, crearla (Llamar a PharoBotGlobalEnvironment)"
	"y agregarla a la lista de clases del ambiente (ideal con Announcements)"
	self flag: #TODO.
	self shouldBeImplemented.